# Sample live service config used in integration tests
# Fields:
#   IMAGE    - Docker image and tag to deploy
#   REPLICAS - Desired number of running instances
#   ENV      - Deployment environment (e.g. production, staging)
#   PORT     - Port the service listens on
IMAGE=nginx:1.25
REPLICAS=3
ENV=production
PORT=8080
